<UserControl x:Class="jPod.CreatePlayListview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
        <StackPanel Orientation="Horizontal">
            <TextBox Width="100" Margin="5" Text="{Binding PlayListName, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Button Width="70" Margin="5" Command="{Binding CreatePlayListCommand}">Create</Button>
        </StackPanel>
</UserControl>
